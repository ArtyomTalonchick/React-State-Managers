import styled from "styled-components";

const S = {
    container: styled.div`
        display: flex;
        align-items: stretch;
        gap: 24px;
        overflow: auto;
    `
};

export default S;
